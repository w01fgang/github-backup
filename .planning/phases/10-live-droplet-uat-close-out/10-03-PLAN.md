---
phase: 10-live-droplet-uat-close-out
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - .planning/phases/10-live-droplet-uat-close-out/10-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - VALID-01
  - VALID-02
  - VALID-03
tags:
  - uat
  - triage
  - phase-closure
must_haves:
  truths:
    - "D-05 severity gate: For EACH `result: failed:` entry recorded by 10-02, this plan applies the D-05 routing rule. `blocking:` → spawn a new phase (e.g. Phase 11) by adding a row to ROADMAP.md + creating a phase directory under `.planning/phases/<NN>-<slug>/` with a stub `<NN>-CONTEXT.md`; the spawned phase blocks Phase 10 close until it ships. `env:` / `doc:` / `infra:` → fix INLINE during this plan (commit the fix as part of the phase) and record in `10-VERIFICATION.md` §Inline Fixes. No new phase spawned for non-blocking."
    - "D-06 triage audit: `10-VERIFICATION.md` §Failure Triage Table has every row's Resolution column updated from `TBD — plan 10-03` to either: (a) `Spawned Phase NN — <slug>` (link to ROADMAP), or (b) `Inline fix — commit <sha>` for non-blocking. Empty table = no failures; this plan still runs to flip frontmatter `status:` and finalize closure markers."
    - "ROADMAP.md update: Phase 10 row transitions from `[ ]` to `[x]` IFF every failure is resolved (every Failure Triage row has a non-TBD Resolution) AND every spawned bug-fix phase from this plan is itself `[x]` closed. Otherwise Phase 10 row stays `[ ]` and ROADMAP gets the new spawned phase row(s) appended, with Phase 10's `Depends on:` field updated to add the new phase numbers."
    - "STATE.md final-state: rows that 10-02 left as `provisionally-resolved` or `targeted with Pending Phase NN spawn note` are re-checked. If the spawned phase has shipped by the time this plan runs (parallel execution scenario), flip to `resolved`. Otherwise leave as `targeted` with the spawn pointer."
    - "10-VERIFICATION.md frontmatter: `status:` transitions to one of `passed` (no failures ever existed), `passed-with-inline-fixes` (only non-blocking failures, all inline-fixed in this plan), or `blocked-on-spawn` (at least one blocking failure with the spawned phase not yet closed). The status is the canonical signal for Phase 10 closure."
    - "Non-blocking inline fixes touch only what the failure demanded — surgical (Rule 3, CLAUDE.md). A `doc:` failure fixes the documented step; an `env:` failure adds the missing env var to `.env.example` or README; an `infra:` failure adds a retry-loop note or a `wait-for-X` helper. Each inline fix is its own logically-separated commit (multiple commits in this plan is OK; reviewers can read each separately)."
    - "Spawned phase stubs: each spawned bug-fix phase has, at minimum, (1) a row in ROADMAP.md with goal + depends_on + requirements; (2) a directory `.planning/phases/<NN>-<slug>/`; (3) a stub `<NN>-CONTEXT.md` with at least Phase Boundary + 1-2 locked decisions naming the actual bug; (4) STATE.md table entry under `Plus emergent v1.1 reqs` (or v1.2 if past v1.1 cut) pointing back to Phase 10 as the discoverer. The stub is NOT a full plan — it's the seed for a later `/gsd-discuss-phase NN` run."
  must_show:
    - "Every row of 10-VERIFICATION.md §Failure Triage Table has a Resolution column matching either `^Spawned Phase [0-9]+` or `^Inline fix — commit [0-9a-f]{7,40}` or `^No fix — accepted as known-issue` (the last only with explicit operator approval, very rare)."
    - "If §Failure Triage Table has zero rows: 10-VERIFICATION.md frontmatter `status: passed` AND ROADMAP.md Phase 10 row is `[x]`."
    - "If §Failure Triage Table has only non-blocking rows: 10-VERIFICATION.md frontmatter `status: passed-with-inline-fixes`, §Inline Fixes has at least one entry per non-blocking failure, ROADMAP.md Phase 10 row is `[x]`, no new ROADMAP phase rows added."
    - "If §Failure Triage Table has any `blocking:` rows: 10-VERIFICATION.md frontmatter `status: blocked-on-spawn`, §Spawned Bug-Fix Phases has at least one entry per blocking failure, ROADMAP.md has new phase rows (each with `[ ]`), Phase 10 row stays `[ ]`, Phase 10 `Depends on:` updated to include the spawned phase numbers."
    - "STATE.md final state: every v1.0-deferred Phase 10 row is either `resolved` (canonical closure path) or `targeted` with an explicit spawned-phase pointer (`Pending Phase NN`). No row remains as bare `provisionally-resolved`."
    - "For any spawned phase: `test -d .planning/phases/<NN>-<slug>/` exits 0 AND `test -f .planning/phases/<NN>-<slug>/<NN>-CONTEXT.md` exits 0."
    - "Inline-fix commits: each non-blocking failure has a corresponding commit referenced in §Inline Fixes. `git log --oneline ${BASE_SHA}..HEAD | grep -c .` matches the count of §Inline Fixes entries plus at most 2 phase-meta commits (the spawned-phase stub commit and the final ROADMAP/STATE/VERIFICATION update commit)."

<context>
<files_to_read_before_planning>
- .planning/phases/10-live-droplet-uat-close-out/10-CONTEXT.md (D-05, D-06)
- .planning/phases/10-live-droplet-uat-close-out/10-VERIFICATION.md (filled in by 10-02 — read §Failure Triage Table, §Inline Fixes, §Spawned Bug-Fix Phases, §STATE.md Gap Resolution)
- .planning/STATE.md (current state of Deferred Items table)
- .planning/ROADMAP.md (Phase 10 row + format of existing phase rows + Coverage table)
- .planning/REQUIREMENTS.md (for any new spawned phase needing a REQ-ID — the spawned phase becomes a v1.2 candidate if v1.1 is the active milestone; check `.planning/MILESTONES.md` for the active milestone)
- For any spawned phase: read the underlying failed scenario's `expected:` block to inform the spawned-phase stub's Phase Boundary.
</files_to_read_before_planning>

<failure_classification_routing>
| Classification | Action in this plan | Output |
|----------------|---------------------|--------|
| `blocking:` | (a) Add ROADMAP.md row for spawned phase. (b) Create `.planning/phases/<NN>-<slug>/<NN>-CONTEXT.md` stub. (c) Add STATE.md row under "emergent v1.1 reqs" (or v1.2). (d) Update Phase 10 ROADMAP row's `Depends on:` to include the new phase. (e) Update 10-VERIFICATION.md §Spawned Bug-Fix Phases + §Failure Triage Resolution col. Phase 10 stays `[ ]`. | New phase stub + ROADMAP update |
| `env:` | Inline fix: add the missing prereq to README / `.env.example` / `config.json` sample. Commit with `fix(10): env: <one-line>`. | §Inline Fixes entry + commit ref |
| `doc:` | Inline fix: correct the doc typo or outdated example. Commit with `docs(10): fix: <one-line>`. | §Inline Fixes entry + commit ref |
| `infra:` | Inline fix: add a retry-loop, a `wait-for-X` helper, OR a README note documenting the wall-clock expectation. Commit with `fix(10): infra: <one-line>`. | §Inline Fixes entry + commit ref |

**Surgical-change rule (CLAUDE.md Rule 3)**: each inline fix touches ONLY the file(s) that directly cause the failure. No drive-by refactors, no comment cleanups in adjacent code.
</failure_classification_routing>

<spawned_phase_stub_template>
For each `blocking:` failure that needs a new phase, create the stub at `.planning/phases/<NN>-<slug>/<NN>-CONTEXT.md`:

```markdown
# Phase <NN>: <Slug> - Context

**Gathered:** <YYYY-MM-DD>
**Status:** Ready for discussion (stub from Phase 10 — VALID-XX discovered)

<domain>
## Phase Boundary

Fix the blocking failure surfaced by Phase 10 scenario `<id>`: <one-paragraph summary of what failed, quoting the runner's reason verbatim>. Out of scope: <list adjacent things this phase WILL NOT touch>.

</domain>

<decisions>
## Implementation Decisions

(Stub — full decision set comes from `/gsd-discuss-phase <NN>`.)

- **D-01 (BUG-LOCATION):** The defect is in `<file-or-area>` based on the failure reason. Targeted fix scope: <1-2 line description>.
- **D-02 (REGRESSION-GUARD):** Add a `verify:phase-<orig-N>` (or `verify:phase-<NN>`) assertion that fails when this regression recurs.

</decisions>

<canonical_refs>
## Canonical References

- `.planning/phases/10-live-droplet-uat-close-out/10-VERIFICATION.md` §Failure Triage Table row `<id>` — the discovery point.
- The original phase's UAT file documenting the contract that was broken.
- The source file(s) implicated by the failure.

</canonical_refs>
```

The stub is intentionally thin. `/gsd-discuss-phase <NN>` later expands it into the full CONTEXT.md.
</spawned_phase_stub_template>

<roadmap_phase_row_template>
For each spawned phase, append a new row to ROADMAP.md under `### v1.1 Production hardening` (or `### v1.2 ...` if v1.1 is closed by this point):

```markdown
- [ ] **Phase <NN>: <Slug>** — <Goal one-liner>. Discovered by Phase 10 (VALID-XX); see `.planning/phases/10-live-droplet-uat-close-out/10-VERIFICATION.md` §Failure Triage row `<scenario-id>`.
```

Also add a Coverage table row in ROADMAP.md `## Coverage` section pointing the new REQ-ID (if one is assigned) at the new phase number, OR a footnote `Phase NN — emergent from Phase 10 triage, no standalone REQ-ID`. Decide based on whether the failure mode warrants a top-level requirement.

The new phase row must also include `**Depends on**:` and `**Requirements**:` and `**Success Criteria**:` — see the existing Phase 8 / 9 entries for format. For the stub, success criteria can be ONE line: "The Phase 10 scenario `<id>` passes when re-run after this phase's plans execute." `/gsd-discuss-phase <NN>` will refine.
</roadmap_phase_row_template>
</context>

<tasks>

<task id="03-01">
<title>Read 10-VERIFICATION.md Failure Triage Table; build the action plan per row</title>
<read_first>
- .planning/phases/10-live-droplet-uat-close-out/10-VERIFICATION.md (full §Failure Triage Table + §STATE.md Gap Resolution + frontmatter `status:`)
- .planning/phases/10-live-droplet-uat-close-out/10-uat-classifications.tsv (if exists from 10-02)
</read_first>
<action>
This task produces NO file changes — it builds an internal action list. For each row in §Failure Triage Table, determine the routing per `<failure_classification_routing>`:

1. Enumerate every failure row. Group by classification.
2. For each `blocking:` row, compute:
   - Proposed phase number `<NN>` = max-current-phase + 1 (read ROADMAP.md, find highest existing phase, add 1; if multiple blockers spawn, assign sequential numbers).
   - Proposed slug: `fix-<scenario-id>-<2-3-word-summary>` (lowercase, hyphen-separated, max 40 chars).
   - One-line goal: derived from the failure reason + the original scenario's `expected:` text.
3. For each non-blocking row, identify the file(s) that need editing for the inline fix. Surface them as a sub-list.
4. If the Triage table is empty (zero failures): skip directly to task 03-05 (closure markers only — no spawned phases, no inline fixes).

Write the plan to stdout (or a temp file `10-triage-plan.txt` in the phase dir). 03-02..03-04 consume it.
</action>
<acceptance_criteria>
- Task produces a structured action list (printed or written to `.planning/phases/10-live-droplet-uat-close-out/10-triage-plan.txt`) listing: blocking-failure count, non-blocking count, proposed phase numbers + slugs for each blocker, inline-fix target files for each non-blocker.
- No file outside the phase directory is modified.
- If §Failure Triage Table is empty: action list contains "no failures — proceed to 03-05".
</acceptance_criteria>
</task>

<task id="03-02">
<title>Apply inline fixes for non-blocking failures (env / doc / infra)</title>
<read_first>
- The action list from 03-01
- For each non-blocking row: the file(s) implicated (e.g. README.md, .env.example, config.json sample, scripts/<file>.ts comment block)
</read_first>
<action>
**Skip if** the action list has zero non-blocking failures.

For each non-blocking failure, apply the minimal fix per `<failure_classification_routing>`. One commit per logical fix (multiple commits per task OK):

- `env:` — add the missing prereq to the appropriate config doc. Commit `fix(10): env: <one-line summary>`.
- `doc:` — correct typo / outdated example. Commit `docs(10): fix: <one-line summary>`.
- `infra:` — add retry-loop / wait helper / README timing note. Commit `fix(10): infra: <one-line summary>`.

**Surgical-change rule**: touch ONLY the file(s) that directly cause the failure. Skip drive-by refactors, comment cleanups, formatting changes in unrelated regions.

After each commit, record `<one-line summary> (commit <sha>)` to a running list (file `.planning/phases/10-live-droplet-uat-close-out/10-inline-fix-log.tsv`) — 03-05 reads this to populate §Inline Fixes in 10-VERIFICATION.md.

Each commit must pass the Phase 8 pre-commit hook without `--no-verify`. If the hook fails because the inline fix triggers a manifest re-sync requirement, RUN `npm run sync:readme` and include the README diff in the same commit (`fix(10): env: <summary> + manifest sync`).
</action>
<acceptance_criteria>
- For every non-blocking failure in the action list, exactly one commit exists in `git log --oneline ${BASE_SHA}..HEAD` with a message matching one of: `^fix\(10\): env:`, `^docs\(10\): fix:`, `^fix\(10\): infra:`.
- `.planning/phases/10-live-droplet-uat-close-out/10-inline-fix-log.tsv` exists with one row per inline fix: `<scenario-id>\t<classification>\t<one-line-summary>\t<commit-sha>`.
- Each inline-fix commit's `git show --stat <sha>` shows EXACTLY the file(s) targeted by that failure's classification + at most one of `README.md` (manifest-sync side-effect). No unrelated files in any inline-fix commit.
- No commit in this task uses `--no-verify`.
</acceptance_criteria>
</task>

<task id="03-03">
<title>Spawn bug-fix phase stubs for blocking failures</title>
<read_first>
- The action list from 03-01
- .planning/ROADMAP.md (existing phase row format)
- For each blocker: the original scenario's `expected:` text from the HUMAN-UAT file
- `<spawned_phase_stub_template>` from this plan's context
- `<roadmap_phase_row_template>` from this plan's context
</read_first>
<action>
**Skip if** the action list has zero blocking failures.

For each blocking failure:

1. Create directory `.planning/phases/<NN>-<slug>/`.
2. Write `<NN>-CONTEXT.md` using `<spawned_phase_stub_template>` — fill in `<NN>`, `<slug>`, `<scenario-id>`, the failure reason verbatim from runner output, and the original `expected:` text for Phase Boundary.
3. Append a new row to `.planning/ROADMAP.md` under the active milestone heading (likely `### v1.1 Production hardening` or `### v1.2 …` if v1.1 is past). Format per `<roadmap_phase_row_template>`. Also append Phase Details section + Coverage row.
4. Update `.planning/STATE.md` under `Plus emergent v1.1 reqs not in v1.0 deferred set` (or v1.2 equivalent) — add a row: `| todo | <bug summary> | targeted | Phase <NN> (<slug>) |`.
5. Update existing Phase 10 ROADMAP row's `**Depends on**:` field to include the new phase number — Phase 10 cannot close until the spawned phase closes (D-05 contract).

All edits in this task land in a SINGLE commit per spawned phase: `feat(<NN>): spawn fix-phase stub from Phase 10 UAT failure (VALID-XX)`. If multiple blockers spawn multiple phases, multiple commits — one per spawn.

Record each spawn in `.planning/phases/10-live-droplet-uat-close-out/10-spawn-log.tsv`: `<scenario-id>\t<spawned-phase-NN>\t<slug>\t<commit-sha>` — 03-05 reads this for §Spawned Bug-Fix Phases.
</action>
<acceptance_criteria>
- For every blocking failure: `test -d .planning/phases/<NN>-<slug>/` exits 0 AND `test -f .planning/phases/<NN>-<slug>/<NN>-CONTEXT.md` exits 0.
- ROADMAP.md has a new `- [ ] **Phase <NN>:` row for each blocker.
- ROADMAP.md Phase 10 row's `**Depends on**:` field lists every newly-spawned phase number.
- STATE.md has a new `targeted` row for each blocker under emergent reqs.
- `.planning/phases/10-live-droplet-uat-close-out/10-spawn-log.tsv` has one row per blocker.
- One git commit per spawn, message matching `^feat\(<NN>\): spawn fix-phase stub from Phase 10 UAT failure`.
- No commit uses `--no-verify`.
</acceptance_criteria>
</task>

<task id="03-04">
<title>Update 10-VERIFICATION.md sections to reflect triage outcomes</title>
<read_first>
- .planning/phases/10-live-droplet-uat-close-out/10-VERIFICATION.md (current state)
- .planning/phases/10-live-droplet-uat-close-out/10-inline-fix-log.tsv (if exists, from 03-02)
- .planning/phases/10-live-droplet-uat-close-out/10-spawn-log.tsv (if exists, from 03-03)
</read_first>
<action>
Make 3 edits to `.planning/phases/10-live-droplet-uat-close-out/10-VERIFICATION.md`:

**Edit A — §Failure Triage Table Resolution column**:
For each row whose `id` is in `10-inline-fix-log.tsv`: set Resolution = `Inline fix — commit <sha>`.
For each row whose `id` is in `10-spawn-log.tsv`: set Resolution = `Spawned Phase <NN> — <slug>`.
If a row remains with Resolution `TBD — plan 10-03` after this edit, that's a bug: surface and stop.

**Edit B — §Inline Fixes section**:
Replace any placeholder with one bullet per row of `10-inline-fix-log.tsv`:
```
- <scenario-id> (<classification>): <one-line-summary> (commit <sha>)
```
If the file has zero inline fixes, write `(none — no non-blocking failures recorded)` so the section is not blank.

**Edit C — §Spawned Bug-Fix Phases section**:
Replace any placeholder with one bullet per row of `10-spawn-log.tsv`:
```
- Phase <NN> — <slug>: spawned from `<scenario-id>` (blocking: <one-line failure reason>). ROADMAP row commit <sha>.
```
If zero spawns, write `(none — no blocking failures recorded)`.

Commit message: `docs(10): record triage outcomes in 10-VERIFICATION.md`.
</action>
<acceptance_criteria>
- `grep -c 'TBD — plan 10-03' .planning/phases/10-live-droplet-uat-close-out/10-VERIFICATION.md` returns 0 (no unresolved Resolution cells).
- Every §Failure Triage Resolution cell matches: `^(Spawned Phase [0-9]+|Inline fix — commit [0-9a-f]{7,40}|No fix — accepted as known-issue)$`.
- §Inline Fixes section: line count of bulleted entries equals row count of `10-inline-fix-log.tsv` (or contains `(none — ...)`  marker if no fixes).
- §Spawned Bug-Fix Phases section: line count of bulleted entries equals row count of `10-spawn-log.tsv` (or `(none — ...)` marker).
- Single commit produced for this task: `git log -1 --format='%s'` matches `^docs\(10\): record triage outcomes in 10-VERIFICATION.md`.
</acceptance_criteria>
</task>

<task id="03-05">
<title>Final closure: flip 10-VERIFICATION.md status, STATE.md rows, ROADMAP Phase 10 checkbox</title>
<read_first>
- .planning/phases/10-live-droplet-uat-close-out/10-VERIFICATION.md (post-03-04 state)
- .planning/STATE.md (current state of v1.0-deferred Phase 10 rows + emergent rows added by 03-03)
- .planning/ROADMAP.md (Phase 10 row + spawned phase rows added by 03-03)
</read_first>
<action>
Determine final closure status by inspecting §Failure Triage Table:

- **No failures ever existed** (table empty): `status: passed`. Phase 10 ROADMAP row → `[x]`. Every STATE.md Phase 10 row → `resolved`. No spawned phases.
- **Only non-blocking failures, all inline-fixed**: `status: passed-with-inline-fixes`. Phase 10 ROADMAP row → `[x]`. Every STATE.md Phase 10 row → `resolved` (each gap fully closed because non-blocking fixes count as full resolution per D-05). No new ROADMAP phase rows.
- **Any blocking failure with spawned phase NOT yet closed**: `status: blocked-on-spawn`. Phase 10 ROADMAP row stays `[ ]`. STATE.md rows for the affected gap stay `targeted` with `Pending Phase <NN>` note; STATE.md rows for OTHER gaps (whose phase had only non-blocking failures) flip to `resolved`. Phase 10 close happens later, in a separate commit, after the spawned phase ships.
- **Any blocking failure with spawned phase ALREADY closed** (parallel-execution scenario — rare but possible): `status: passed-with-fixes-shipped`. Phase 10 ROADMAP row → `[x]`. STATE.md rows for the affected gap flip to `resolved` with reference to the spawned phase's commit.

Make 3 file edits:

1. `.planning/phases/10-live-droplet-uat-close-out/10-VERIFICATION.md` — frontmatter `status:` flipped per the above. Also add a trailing footer block:
   ```
   ---
   Closed: 2026-MM-DD
   Closer: Phase 10 plan 10-03 (commit <pending>)
   ```
   The `<pending>` placeholder is set after commit; either accept the placeholder OR amend in a follow-up commit referencing the closing-commit SHA. For automation simplicity, leave the placeholder.

2. `.planning/STATE.md` — flip every Phase-10-related row that's still `targeted` or `provisionally-resolved` to its final state per the routing above. Other rows untouched.

3. `.planning/ROADMAP.md` — toggle Phase 10 row checkbox per the routing. If `[x]`, also update the bottom Progress table:
   ```
   | 10. Live-droplet UAT close-out | v1.1 | <K>/<K> | Complete | 2026-MM-DD |
   ```
   where `K` = total plans in Phase 10 directory (currently 3: 10-01, 10-02, 10-03). Stays `[ ]` + `Not started` → leave Progress table row unchanged with a note like `(blocked on Phase <NN> spawn)`.

Single commit covering all 3 file edits: `docs(10): close-out — status=<X>, ROADMAP=[<x|space>], STATE.md=<K>/5 resolved`.

Don't use `--no-verify`. Pre-commit hook (Phase 8 manifest sync) must pass.
</action>
<acceptance_criteria>
- `grep -E '^status: (passed|passed-with-inline-fixes|passed-with-fixes-shipped|blocked-on-spawn)$' .planning/phases/10-live-droplet-uat-close-out/10-VERIFICATION.md` returns exactly 1 match.
- If status is `passed*`: `grep -c '^- \[x\] \*\*Phase 10:' .planning/ROADMAP.md` returns >= 1.
- If status is `blocked-on-spawn`: `grep -c '^- \[ \] \*\*Phase 10:' .planning/ROADMAP.md` returns >= 1.
- STATE.md Phase 10 rows: every row is either `status: resolved` OR `status: targeted` with `Pending Phase <NN>` annotation. No `provisionally-resolved` rows remain.
- Single closure commit: `git log -1 --format='%s'` matches `^docs\(10\): close-out`.
- ROADMAP Progress table row for Phase 10 is updated when status is `passed*`.
- Pre-commit hook passed.
</acceptance_criteria>
</task>

</tasks>

<verification>
After all 5 tasks complete:

1. **Triage complete**: every §Failure Triage row has a non-TBD Resolution.
2. **Inline fixes recorded + commits exist**: §Inline Fixes count matches commit count for non-blocking.
3. **Spawns complete (if any)**: every blocking failure has a spawned phase directory + ROADMAP row + STATE.md entry + Phase 10 `Depends on:` updated.
4. **Closure status set**: 10-VERIFICATION.md frontmatter `status:` reflects routing.
5. **ROADMAP synced**: Phase 10 row checkbox + Progress table reflect closure routing.
6. **STATE.md synced**: every v1.0-deferred Phase 10 row has a final state (`resolved` or `targeted with spawn pointer`).
7. **Audit trail preserved**: sidecar TSVs (`10-inline-fix-log.tsv`, `10-spawn-log.tsv`, `10-uat-runner-output.log`) remain in the phase dir as the audit record but are NOT committed.
8. **Phase 10 closes here** if and only if frontmatter `status:` is one of `passed*`. Otherwise Phase 10 stays open with a clear pointer to the spawned phase(s) blocking it.
</verification>

<unblocks>
Phase 10 close (final).
Next milestone iteration (v1.2 planning) — Phase 10's spawned phases (if any) become v1.2 candidates.
</unblocks>
</content>
</invoke>